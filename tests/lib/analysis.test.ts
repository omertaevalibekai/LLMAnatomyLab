import { describe, expect, it } from "vitest"; import { applyTemperature, entropy, jensenShannonDivergence, klDivergence } from "@/lib/analysis"; describe("analysis", () => { it("entropy", () => { expect(entropy([0.5, 0.5])).toBeCloseTo(1, 5); }); it("kl", () => { expect(klDivergence([0.5, 0.5], [0.9, 0.1])).toBeGreaterThan(0); }); it("js", () => { expect(jensenShannonDivergence([0.5, 0.5], [0.5, 0.5])).toBeCloseTo(0, 6); }); it("temperature", () => { const probs = applyTemperature([2, 1, 0], 1); expect(probs.reduce((a,b)=>a+b,0)).toBeCloseTo(1, 6); }); });
