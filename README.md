# LLM Anatomy Lab

> Interactive behavioral interpretability explorer for large language models.

## Run Locally

1. Install dependencies:

```bash
npm install
```

2. Create `.env.local` in the project root:

```env
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
```

3. Start development server:

```bash
npm run dev
```
